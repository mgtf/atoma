{
  "_comment": "ROUND 11 — CONTROL-ARM CALIBRATION ONLY. Identical goal and seed to rounds 5-10, control arm back at the original Opus pin, atomaRuns 0. Exists so the three-model table (Opus/Sonnet/Haiku) is anchored on one day rather than quoting round 8 across five days. The driver report H1 line is VACUOUS here — analyse() receives an empty treatment series. See PROTOCOL.md#round-10.",
  "primary": {
    "id": "wclite-maint",
    "seed": "benchmark/seeds/wclite",
    "goal": "The workspace already contains a working wclite CLI (wclite.js), its README.md documenting five verified invocations, and .atoma-probes.json recording what each of them produced. Make one change: --chars must now count characters EXCLUDING trailing newlines at the end of the file. Apply that change to wclite.js. Then re-verify that every invocation documented in README.md and recorded in .atoma-probes.json still behaves as recorded, and update the README and the manifest only for what legitimately changed. Do not rewrite the CLI, do not add features, and leave every other documented behaviour exactly as it is."
  },
  "heldOut": {
    "id": "wclite-maint-2",
    "seed": "benchmark/seeds/wclite",
    "goal": "The workspace already contains a working wclite CLI (wclite.js), its README.md documenting five verified invocations, and .atoma-probes.json recording what each of them produced. Make one change: passing a directory instead of a file must exit 2 with the message 'wclite: not a regular file: <path>' rather than the current read error. Apply that change to wclite.js. Then re-verify that every invocation documented in README.md and recorded in .atoma-probes.json still behaves as recorded, and update the README and the manifest only for what legitimately changed. Do not rewrite the CLI and do not add features."
  },
  "baselineRuns": 3,
  "atomaRuns": 0,
  "heldOutBaselineRuns": 0,
  "heldOutAtomaRuns": 0,
  "timeoutMs": 900000
}
