name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  # Never discard the worker proof for a commit already on main.
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  core:
    name: Hermetic checks (Docker integration skipped)
    runs-on: ubuntu-latest
    # The GPU smoke runs on a CPU rasteriser here — measured 3433ms per frame on
    # the runner against 16.7ms on a Metal-backed developer machine — so the
    # browser arms alone take ~9 minutes of the step. 15 was not enough to reach
    # the end of the check that has to pass.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci
      - run: npm run release:check

  worker:
    name: Fresh worker image
    needs: core
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci
      - run: npm run build:worker:dev
      - run: test -f dist/tools/worker.js && test -f dist/contracts/probeManifest.js
      - run: npx vitest run tests/container-isolation.test.ts
        env:
          CI_REQUIRE_DOCKER: "1"
