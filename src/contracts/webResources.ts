/** Public assets supported by the default run and preview network policy. */
export const DEFAULT_WEB_RESOURCE_HOSTS: readonly string[] = [
  'fonts.googleapis.com',
  'fonts.gstatic.com',
  'cdn.jsdelivr.net',
  'unpkg.com',
];
