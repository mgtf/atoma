# CLA signatures

This orphan branch holds the contributor licence agreement signatures that
the `CLA` workflow (`.github/workflows/cla.yml`) records. The workflow creates
and updates `signatures/cla-v1.json` itself; do not edit that file by hand,
and keep this branch unprotected so the workflow can commit to it.
